############################################################
# ⚠️ DEPRECATED ⚠️
# This Docker Compose override is no longer maintained.
# Use the npm-based wp-env workflow instead.
#
# See ../bricks-etch-migration/README.md for setup instructions.
# Quickstart:
#   cd ../bricks-etch-migration
#   npm run dev
############################################################

version: '3.8'

# Example docker-compose override file for local customizations
# Copy this file to docker-compose.override.yml to apply local changes
# The override file is gitignored and won't be committed

services:
  # Example: Change ports if 8080/8081 are already in use
  # bricks-wp:
  #   ports:
  #     - "9080:80"
  # 
  # etch-wp:
  #   ports:
  #     - "9081:80"

  # Example: Mount additional themes directory
  # bricks-wp:
  #   volumes:
  #     - ./themes:/var/www/html/wp-content/themes

  # Example: Increase PHP memory limit
  # bricks-wp:
  #   environment:
  #     - PHP_MEMORY_LIMIT=512M
  # 
  # etch-wp:
  #   environment:
  #     - PHP_MEMORY_LIMIT=512M

  # Example: Enable Xdebug for development
  # bricks-wp:
  #   environment:
  #     - XDEBUG_MODE=develop,debug
  #     - XDEBUG_CONFIG=client_host=host.docker.internal
  # 
  # etch-wp:
  #   environment:
  #     - XDEBUG_MODE=develop,debug
  #     - XDEBUG_CONFIG=client_host=host.docker.internal
